type: pages
headline: Articles
parent: kirby.page("articles")
layout: cards
info: "{{ page.author }} {{ page.category }}"
sortBy: date desc
empty: No articles yet
image:
  query: page.cover.toFile()
  cover: true
templates:
  - article
