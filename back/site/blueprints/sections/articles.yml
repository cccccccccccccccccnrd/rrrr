type: pages
label: Articles
layout: cards
parent: kirby.page("articles")
info: "{{ page.author }}<br>in {{ site.find(page.category).title }}"
empty: No articles yet
template: article
image:
  query: page.cover.toFile()
  cover: true
