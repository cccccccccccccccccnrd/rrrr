type: pages
headline: Articles
parent: kirby.page("articles")
text: "{{ page.title }}"
info: "{{ page.published }}"
layout: cards
empty: No articles yet
sortBy: date desc
image:
  query: page.cover
  cover: true
  ratio: 1
templates:
  - article
